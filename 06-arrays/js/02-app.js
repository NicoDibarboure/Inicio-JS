const numeros = [10, 20, 30, 40, 50, [1, 2, 3]];
console.log(numeros);
console.table(numeros);

// Acceder a los valores de un array

console.log(numeros[2]); // De esta manera accedemos al valor 30 que está en la posición 2.
console.log(numeros[5][2]); // De esta manera accedemos al valor 3 que está dentro del array [1, 2, 3]

function saluda(
  nombre = "No ingreso el nombre", // Se le asigna un valor por defecto.
  apellido = "No ingreso el apellido"
) {
  console.log(`Hola ${nombre} ${apellido}`);
}

saluda("Nicolas", "Dibarboure");

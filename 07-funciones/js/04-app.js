const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); // Esto es una función que convierte un string a un número entero.

console.log(numero1.toString()); // Esto es un metodo que convierte un número entero a un string.

function sumar() {
  console.log(2 + 2);
}
sumar();

let i = 0; // Inicializar el While Loop

while (i < 10) {
  if (i % 2 === 0) {
    console.log(`El numero ${i} es par.`);
  } else {
    console.log(`El numero ${i} es impar.`);
  }

  i++; // Incrementar el While Loop
}

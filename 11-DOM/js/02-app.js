// Seleccionar elementos por su clase.

const header = document.getElementsByClassName("header");

console.log(header);

const hero = document.getElementsByClassName("hero");

console.log(hero);

// Si las clases existen más de una vez, se seleccionan todas las clases con ese nombre.

const contenedores = document.getElementsByClassName("hero");

console.log(hero);

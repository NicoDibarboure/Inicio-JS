let resultado = (8 / 2) * (2 + 2);
console.log(resultado); // 16
// En este ejemplo, la división de 8 entre 2(2 + 2) es 16. El resultado es 16.

let resultado2 = 8 / (2 * (2 + 2));
console.log(resultado2); // 1
// En este ejemplo, la división de 8 entre 2(2 + 2) es 1. El resultado es 1.

// TODO || En JavaScript, el orden de las operaciones es importante. Si no se tiene en cuenta el orden de las operaciones, el resultado puede ser diferente. JavaScript sigue el orden de las operaciones matemáticas. El orden de las operaciones es el siguiente:

// expresion ? 'si es true' : 'si es false';

let edad = 18;
let acceso = edad > 17 ? "Acceso permitido" : "Acceso denegado";
console.log(acceso); // Acceso permitido
// En este ejemplo, si la edad es mayor que 17, el acceso es permitido. De lo contrario, el acceso es denegado.
// El operador ternario es una forma abreviada de escribir una declaración if...else.

let acceso2 = edad > 27 ? "Acceso permitido" : "Acceso denegado";
console.log(acceso2); // Acceso denegado
